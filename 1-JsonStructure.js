/* JSON (JavaScript Object Notation)
    Is a file format that is commonly used
    in transporting data, via API or other means. 

    A JSON can have multiple JSON DAta by using 
    commas as separators.
    JSONs should be surrounded by {}.

    A JSON Data should have a key | value pair and colon ":" 
    in the middle.
    JSONs should be surrounded by {}.
*/

// JSON Example
let person = {
    firstName: "Arnel",
    lastName: "Rose",
    age: 43,
    sex: "M",
    bloodType: "O"
}

console.log(person);